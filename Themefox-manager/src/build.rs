/*
use clap_generate::{
    generate, generators::Bash, generators::Elvish, generators::Fish, generators::PowerShell,
    generators::Zsh,
};
use std::io;

pub fn documentation(app: clap::build::app::App) {
    generate::<Bash, /* Fish, Zsh, Elvish, PowerShell,*/ _>(
        &mut app,
        "themefox-manager",
        &mut io::stdout(),
    );
}
*/
